# Contributing

感谢你帮助改进这个技能集合。

## 提交前

- 保持每个技能的 `SKILL.md`、`USAGE.md` 和目录名一致。
- 在 `SKILL.md` 的 frontmatter 中填写准确的 `name`、`description` 和 `license`。
- 为新增或修改的技能更新 `CATALOG.md`，包括用途、目录和调用名。
- 不要提交 API Key、个人凭证、真实用户素材或生成服务的私密链接。
- 保留原作者署名、来源和适用许可。

## 提交方式

1. Fork 仓库并创建简短的功能分支。
2. 修改文档或技能内容，并说明使用场景与验证方式。
3. 在本地运行与 CI 相同的目录和链接检查。
4. 提交 Pull Request，描述改动、来源和可能的兼容性影响。

小而明确的改动更容易审核。涉及多个技能时，请说明它们之间的关系。
