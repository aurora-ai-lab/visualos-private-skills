# GSAP for React

Category: **UI GPT SKIL**

## Purpose

Integrate GSAP into React with scoped animations and lifecycle cleanup.

## Invocation

Installed skill name: `gsap-react`.

```text
$gsap-react Add scoped GSAP animation to this React component with cleanup.
```

## Files

- [Skill instructions](SKILL.md)
- Repository folder: `gsap-skills/gsap-react`

The invocation name comes from the skill frontmatter, not the folder label. Image generation requires an available image-generation tool; prompt-only requests do not imply generation.
