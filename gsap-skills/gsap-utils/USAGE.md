# GSAP Utilities

Category: **UI GPT SKIL**

## Purpose

Use interpolation, mapping, clamping, snapping and related helpers.

## Invocation

Installed skill name: `gsap-utils`.

```text
$gsap-utils Map pointer position to a bounded tilt with GSAP utilities.
```

## Files

- [Skill instructions](SKILL.md)
- Repository folder: `gsap-skills/gsap-utils`

The invocation name comes from the skill frontmatter, not the folder label. Image generation requires an available image-generation tool; prompt-only requests do not imply generation.
